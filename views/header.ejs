<nav class="bg-white shadow-sm">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex items-center h-16 justify-between">
      <div class="flex">
        <div class="flex flex-shrink-0 items-center">
          <img src='../public/images/logo.png' width="100%"/>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <!-- Current: "border-indigo-500 text-gray-900", Default: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700" -->
          <a
            href="/"
            class="inline-flex items-center border-b-2 border-transparent px-1 pt-1 text-sm font-medium text-green-500 hover:border-gray-300 hover:text-gray-700"
            >Players</a
          >
          <a
            href="/nations"
            class="inline-flex items-center border-b-2 border-transparent px-1 pt-1 text-sm font-medium text-green-500 hover:border-gray-300 hover:text-gray-700"
            >Nations</a
          >
          <% if (isAuth==true && isAdmin==true) { %>
          <a
            href="/user/accounts"
            class="inline-flex items-center border-b-2 border-transparent px-1 pt-1 text-sm font-medium text-green-500 hover:border-gray-300 hover:text-gray-700"
            >Accounts</a
          >
          <% } %>
        </div>
      </div>

      <div class="flex gap-3">
        <!-- ------------------------- -->
        <% if (isAuth == true) { %>
        <!-- -------- -->
        <div class="flex gap-4">
          <div>Welcome, <%= user.username%></div>
          <a
            href="/user/profile"
            class="inline-flex items-center rounded border border-transparent bg-green-700 px-2.5 py-1.5 text-xs font-medium text-white shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
            >My profile</a
          >
          <a
            href="/user/logout"
            class="inline-flex items-center rounded border border-transparent bg-green-700 px-2.5 py-1.5 text-xs font-medium text-white shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
          >
            Logout
          </a>
        </div>
        <% } %>
        <!-- ------------------ -->
        <% if (isAuth == false) { %>
        <a
          href="/user/login"
          class="inline-flex items-center rounded border border-transparent bg-green-700 px-2.5 py-1.5 text-xs font-medium text-white shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
        >
          Login
        </a>
        <a
          href="/user/register"
          class="inline-flex items-center rounded border border-transparent bg-green-700 px-2.5 py-1.5 text-xs font-medium text-white shadow-sm hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
        >
          Register
        </a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
